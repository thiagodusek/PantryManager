<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PantryManager" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/pantry_primary</item>
        <item name="colorPrimaryDark">@color/pantry_primary_variant</item>
        <item name="colorAccent">@color/pantry_secondary</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/pantry_background</item>
        <item name="android:windowBackground">@color/pantry_background</item>
        
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>

    <style name="Theme.PantryManager" parent="Base.Theme.PantryManager" />
</resources>
